요약:

동적할당의 개념
동적할당의 문제점과 해결책

힙의 개념
힙 정렬 시간 복잡도
힙의 예시

o(1)복잡도 가지는 정렬 알고리즘
c언어에서 연결리스트 추가 방식

자료구조 유의할 점들

연결리스트란? 배열과 연결리스트의 차이


---------------------------------------------------------------------------------
인천대

동적할당,힙,힙소트 시간 복잡도,힙 적용한거 뭐 있냐,자료구조 유의할 점,파이썬 문제 하나
동적할당시 발생할 수 있는 문제점과 해결책

---------------------------------------------------------------------------------
충북대

자율주행,5g관련 이슈

o(1)복잡도 가지는 정렬 알고리즘이 뭐 있냐?
아무리 정렬된 테이블이여도 버블 솔트같은 경우는 o(n)으로도 동작하는데...

연결리스트 추가방식 c
->새노드가 추가될 때 어떤식으로 노드가 생성되는지 궁금해 하신듯?

연결 리스트에서 새로운 노드를 추가할 때는 동적 메모리 할당을 사용하여 새 노드를 생성합니다. 가장 앞에 삽입하는 경우, 먼저 malloc()을 이용해 새 노드를 동적으로 생성한 후 데이터를 저장합니다.

그다음, 새 노드의 next 포인터를 현재 head가 가리키는 노드로 설정하여 기존 리스트와 연결합니다. 이렇게 하면 기존 리스트의 구조를 유지하면서 새 노드가 맨 앞에 추가됩니다.

마지막으로 head를 새 노드로 변경하여, 리스트의 첫 번째 노드가 새로 추가된 노드가 되도록 합니다. 이 과정이 끝나면 맨 앞 삽입이 완료됩니다.

추가로, 동적 메모리를 사용하기 때문에 사용이 끝난 노드는 free()를 호출하여 해제하는 것이 중요합니다.

연결리스트를 어떻게 삽입 삭제하는지

스택에 대한 질문

미적분과 삼각함수가 활용된 프로그램은 무엇이 있는가?
---------------------------------------------------------------------------------
한기대

스택 abc 순서 칠판 코딩, 최적 알고리즘 물어봄

포인터가 뭔지, 가위바위보 했을 때 이길 확률은 몇 분의 몇인지
--------------------------------------------------------------------------------------
부경대

리스트를 연결리스트로 구현할 때와 배열로 구현할 때의 차이와 장단점
->시간복잡도 관련 질문 원한듯?

연결리스트란? 배열과 연결리스트의 차이
